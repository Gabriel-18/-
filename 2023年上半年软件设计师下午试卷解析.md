# 2023 年上半年软件设计师下午试卷详细解析与通关笔记

> 适用目标：把这套“下午案例题”当作模板题来复盘，掌握软考中级软件设计师下午题的固定解题套路，而不是只背答案。  
> 考试结构：试题一至试题四为必答题，试题五、试题六二选一。每题 15 分，满分 75 分。下午题的核心是“读题抽实体、按图补关系、按模式补代码、用术语作答”。  
> 说明：本笔记侧重学习与应试解析。涉及图中编号的题目，若没有原图，应以“题干关键词 + 图中箭头/多重度/继承关系”最终校准。

## 一、下午题整体备考方法

### 1. 做题顺序建议

推荐顺序：

1. **试题二：数据库设计**  
   通常最稳，题干会直接给出实体、属性和业务关系，容易拿基础分。
2. **试题三：UML 类图 / 设计模式**  
   关键词明显，继承、聚合、组合、模式题有固定话术。
3. **试题五或六：程序设计题**  
   Java 或 C++ 选自己熟悉的。先补接口、方法调用、对象创建等语法空。
4. **试题一：数据流图 DFD**  
   需要细读上下文图和 0 层图，容易因为数据流方向丢分。
5. **试题四：算法与数据结构**  
   若代码阅读能力强可以提前做，否则最后处理，先拿能确定的代码空。

### 2. 下午题得分原则

| 题型 | 高分关键 | 常见丢分点 |
| --- | --- | --- |
| DFD 数据流图 | 外部实体、数据存储、加工、数据流名称必须来自题干 | 自己造词；把实体写成数据存储；漏起点终点 |
| ER 图 / 关系模式 | 多对多关系要转关系表；主键、外键要标清 | 漏联系属性；把采购数量放进实体表 |
| UML 类图 | 抽象父类、子类、关键属性、模式意图要对应题干 | 只写类名不写属性；设计模式解释太泛 |
| 算法代码 | 先看变量含义，再看循环不变量 | 只凭语感填代码；不检查边界条件 |
| Java/C++ 代码补全 | 接口方法、策略类创建、调用方向固定 | 大小写、对象名、方法名与题干不一致 |

### 3. 答题话术原则

- 题干已有名词，优先照抄题干名词。
- 问“为什么使用某模式”，必须答出：**变化点、解耦对象、触发条件、好处**。
- 问关系模式，必须写出：**关系名（属性列表）+ 主键 + 外键**。
- 问数据流组成，使用 `数据流 = 数据项1 + 数据项2 + ...` 的形式最稳。

---

## 二、试题一：农事管理服务平台 DFD 解析

### 1. 考点定位

本题考查结构化分析中的 **数据流图（DFD）**：

- 外部实体：系统边界外与系统交互的人或系统。
- 加工：系统内部对数据的处理过程。
- 数据存储：系统内部长期保存的数据集合。
- 数据流：从实体、加工、数据存储之间流动的数据。

做 DFD 题时，不要从业务经验出发乱扩展，只从题干中找词。

### 2. 题干关键词拆解

业务对象可以拆成五组：

1. **人员管理**：平台管理员、租户、农户、人员类型、负责地块。
2. **基地管理**：基地名称、地域、地图、地块。
3. **种植管理**：作物、农事过程、农事活动、实施计划、农事提醒、逾期告警。
4. **投入品管理**：化肥、杀虫剂、投入品信息、实际消耗。
5. **信息服务**：用户、第三方软件、账号、密码、查询条件、农事信息响应。

### 3. 参考答案与解析

#### 问题 1：外部实体 E1-E4

参考对应关系：

| 编号 | 参考答案 | 判断依据 |
| --- | --- | --- |
| E1 | 平台管理员 | 管理租户，是系统外部管理者 |
| E2 | 农户 | 接收农事提醒、记录农事活动 |
| E3 | 租户 | 管理农户、基地、作物、投入品 |
| E4 | 第三方软件 | 通过 APP 接口访问系统 |

解析：

- “平台管理员”“租户”“农户”都是与系统交互的人，因此属于外部实体。
- “第三方软件”不是系统内部模块，而是通过接口调用系统，也属于外部实体。
- “用户”这个词较泛，若图中已分租户、农户、第三方软件，应优先使用更具体的题干名词。

#### 问题 2：数据存储 D1-D4

参考对应关系：

| 编号 | 参考答案 | 可接受写法 |
| --- | --- | --- |
| D1 | 人员表 | 人员信息表、人员记录、人员文件 |
| D2 | 基地及地块数据表 | 基地地块表、基地及地块信息表 |
| D3 | 农事过程表 | 农事过程信息表、农事过程文件 |
| D4 | 农事活动表 | 农事活动记录表、农事活动信息表 |

解析：

- 数据存储通常以“表 / 文件 / 记录 / 信息表”结尾。
- 题目问“使用说明中的词语”，所以不要写成“用户数据库”“业务数据库”这种过宽泛的词。
- “农事过程”和“农事活动”要区分：
  - 农事过程：从种植到采收的计划、流程模板。
  - 农事活动：农户实际执行并记录的活动。

#### 问题 3：补充缺失数据流

由于该题依赖原图箭头和缺口位置，复盘时按以下步骤确定：

1. 先看缺失数据流连接的两端：外部实体、加工、数据存储分别是谁。
2. 回到题干对应功能，找与两端都相关的数据名词。
3. 数据流名称尽量使用题干原词，不自行抽象。

常见应补方向包括：

| 场景 | 可能的数据流 | 起点 | 终点 |
| --- | --- | --- | --- |
| 平台管理员管理租户 | 租户信息 / 人员信息 | 平台管理员 | 人员管理加工 |
| 租户分配农户地块 | 农户负责地块信息 | 租户 | 人员管理或基地管理加工 |
| 农户记录活动 | 农事活动记录 | 农户 | 种植管理加工 / 农事活动表 |
| 系统提醒农户 | 农事提醒 | 种植管理加工 | 农户 |
| 逾期通知租户 | 逾期告警 | 种植管理加工 | 租户 |
| 第三方软件查询 | 农事信息请求 | 第三方软件 | 信息服务加工 |
| 系统返回查询结果 | 农事信息响应 | 信息服务加工 | 第三方软件 |

应试提醒：如果题目要求写“数据流及起点和终点”，答案必须三列齐全；只写数据流名称通常拿不满分。

#### 问题 4：农事信息请求的组成

参考答案：

```text
农事信息请求 = 账号 + 密码 + 查询条件
```

更严谨的写法：

```text
农事信息请求 = 账号 + 密码 + 0{查询条件}*
```

解析：

- 题干明确说第三方软件“提供账号、密码和查询条件”发起请求。
- 又说“无查询条件时默认返回账号下所有信息，多查询条件时返回满足全部条件的信息”，因此查询条件可以是 0 个、1 个或多个。
- 考试中写 `账号 + 密码 + 查询条件` 一般可得基础分；写出 `0{查询条件}*` 更体现数据字典表达能力。

### 4. 本题通关模板

DFD 题遇到不会的图，按这个模板写草稿：

```text
外部实体 = 系统外的人 / 组织 / 其他系统
数据存储 = 业务名词 + 表/文件/记录
数据流 = 题干中可被传递的数据名词
加工 = 动词 + 名词，如 人员管理、种植管理、信息服务
```

---

## 三、试题二：汽车零件采购系统数据库设计解析

### 1. 考点定位

本题考查 **ER 模型到关系模式转换**：

- 实体：供应商、零件、车型。
- 联系：采购。
- 联系属性：采购数量、采购日期。
- 新增需求：门店、销售。

### 2. 原始实体与属性

| 实体 | 属性 | 候选主键 |
| --- | --- | --- |
| 供应商 | 名称、地址、电话 | 名称 |
| 零件 | 编码、名称、价格 | 编码 |
| 车型 | 编号、名称、规格 | 编号 |

题干说明：某个车型的某种零件可以从多家供应商采购，某种零件也可被多个车型采用，某家供应商也可供应多种零件。也就是说，**车型、零件、供应商之间通过采购形成三元联系**。

### 3. 问题 1：补充 ER 图

应补内容：

- 实体：供应商、零件、车型。
- 联系：采购。
- 采购联系连接供应商、零件、车型三个实体。
- 联系属性：采购数量、采购日期。
- 三个实体到采购联系通常都是 `1 : n` 方向，即一个供应商可参与多条采购记录，一个零件可参与多条采购记录，一个车型可参与多条采购记录。

解析：

- 不要把“采购数量、采购日期”放到供应商、零件或车型实体里，因为它们不是某个实体自身的固有属性，而是某次采购关系发生时的属性。
- 三元联系转关系表时，一般把三个参与实体的主键都放入联系表。

### 4. 问题 2：补充关系模式并标注主外键

题干给出：

```text
采购(车型编号, 供应商名称, (a), (b), 采购日期)
```

参考答案：

```text
(a) = 零件编码
(b) = 采购数量
```

完整关系模式建议写为：

```text
供应商(名称, 地址, 电话)
主键：名称

零件(编码, 名称, 价格)
主键：编码

车型(编号, 名称, 规格)
主键：编号

采购(车型编号, 供应商名称, 零件编码, 采购数量, 采购日期)
主键：车型编号 + 供应商名称 + 零件编码 + 采购日期
外键：车型编号 → 车型(编号)
外键：供应商名称 → 供应商(名称)
外键：零件编码 → 零件(编码)
```

关于主键的说明：

- 若题目或图中认为同一车型、供应商、零件只保留一条当前采购记录，则主键可为 `车型编号 + 供应商名称 + 零件编码`。
- 若要记录多次采购历史，`采购日期` 应参与主键或另设采购单号。题干给出了采购日期，考试答题时建议把采购日期作为区分多次采购的属性写清楚。
- 如果标准图中已用下划线指定主键，应以图示为准。

### 5. 问题 3：新增门店销售需求

新增实体：

```text
门店(编号, 地址, 电话)
主键：编号
```

新增联系：销售。

销售连接：门店与车型。

销售联系属性：销售数量、销售日期。

新增关系模式：

```text
销售(车型编号, 门店编号, 销售数量, 销售日期)
主键：车型编号 + 门店编号 + 销售日期
外键：车型编号 → 车型(编号)
外键：门店编号 → 门店(编号)
```

如果题目图中只要求一类车型在一个门店保留一条累计销售记录，则也可能写：

```text
销售(车型编号, 门店编号, 销售数量, 销售日期)
主键：车型编号 + 门店编号
```

但从“销售日期”看，更合理的是记录销售明细或按日期汇总，建议在解析中说明日期是否参与主键。

### 6. 本题通关模板

ER 转关系模式的固定规则：

1. 强实体 → 一张表，实体主键 → 表主键。
2. `1 : n` 联系 → 把 1 端主键加入 n 端作外键。
3. `m : n` 联系 → 单独建联系表，双方主键 + 联系属性。
4. 三元联系 → 单独建联系表，三个实体主键 + 联系属性。
5. 联系属性不能随便放入某个实体表。

---

## 四、试题三：数字图书馆 UML 类图与设计模式解析

### 1. 考点定位

本题考查面向对象分析与设计：

- 从题干识别类名。
- 从“包括 / 分为”识别泛化继承。
- 从“包含 / 收录”识别聚合或组合。
- 从“状态变化后通知关注者”识别观察者模式。

### 2. 类名识别

参考对应关系：

| 编号 | 类名 | 中文含义 | 依据 |
| --- | --- | --- | --- |
| C1 | Resource | 学术资源 | 会议论文、期刊论文、学位论文的父类 |
| C2 | ConferencePaper | 会议论文 | 学术资源的一种 |
| C3 | JournalArticle | 期刊论文 | 学术资源的一种 |
| C4 | Thesis | 学位论文 | 学术资源的一种 |
| C5 | User | 用户 | 学生、教师、其他在职人员的父类 |
| C6 | Student | 学生 | 用户的一种 |
| C7 | Teacher | 教师 | 用户的一种 |
| C8 | Staff | 其他在职人员 | 用户的一种 |

解析：

- 题干说“学术资源包括会议论文、期刊论文以及学位论文”，这是典型泛化关系：`Resource` 是父类，三个具体资源是子类。
- 题干说“系统用户包括学生、教师以及其他在职人员”，也是泛化关系：`User` 是父类，`Student`、`Teacher`、`Staff` 是子类。

### 3. 关键属性识别

参考答案：

```text
Resource：题名、作者、发表时间、来源、被引次数、下载次数
ConferencePaper：会议名称、召开时间、召开地点
JournalArticle：期刊名称、出版月份、期号、主办单位
Thesis：学位类别、毕业学校、专业、指导教师
```

解析：

- 父类放三类资源共有属性：题名、作者、发表时间、来源、被引次数、下载次数。
- 子类只放自己独有属性。
- 如果把会议名称、期刊名称、学位类别都放到 `Resource` 父类，会破坏抽象，因为不是所有资源都有这些属性。

### 4. 设计模式题：他引通知

参考答案：**观察者模式（Observer Pattern）**。

可直接用于答题的 150 字以内答案：

```text
可增加观察者模式。学术资源作为被观察目标，关注该资源的用户作为观察者；当资源的被引次数发生变化时，目标对象通知所有已注册用户，使其及时获得他引变化信息。该模式能降低资源对象与用户通知逻辑之间的耦合，支持用户动态注册和取消关注。
```

解析：

- 关键词“关注”“注册通知”“状态变化”“及时通知”几乎就是观察者模式信号。
- 观察者模式不是为了创建对象，而是为了“一对多通知”。
- 答案必须结合题干说“被引次数变化通知关注用户”，不能只背定义。

### 5. 本题通关模板

UML 类图题快速判断：

```text
A 包括 B/C/D        → A 是父类，B/C/D 是子类
A 由 B 组成         → 组合或聚合
A 使用 B             → 依赖
A 关联 B             → 普通关联
状态变化通知多个对象 → 观察者模式
算法族可替换         → 策略模式
只允许一个实例       → 单例模式
创建复杂对象         → 工厂/抽象工厂/建造者
```

---

## 五、试题四：图的深度优先遍历与算法代码解析

### 1. 考点定位

本题考查数据结构中的 **图的邻接表存储、深度优先搜索 DFS、连通分量统计**。

从参考空可以看出，代码大概率围绕邻接表结点 `p` 遍历邻接边：

```c
p->adjvex       // 当前边指向的邻接顶点编号
p = p->nextarc  // 访问下一条边
DFS(G, 0)       // 从某个顶点开始深度优先遍历
count++         // 统计连通分量或满足条件的数量
return count    // 返回统计结果
```

### 2. 邻接表 DFS 的标准写法

建议掌握下面的模板：

```c
void DFS(ALGraph G, int v) {
    visited[v] = true;
    ArcNode *p = G.vertices[v].firstarc;
    while (p != NULL) {
        int w = p->adjvex;
        if (!visited[w]) {
            DFS(G, w);
        }
        p = p->nextarc;
    }
}
```

逐行理解：

- `visited[v] = true`：标记当前顶点已访问，避免死循环。
- `firstarc`：当前顶点第一条邻接边。
- `p->adjvex`：通过边结点找到邻接顶点编号。
- `p->nextarc`：移动到下一条邻接边。
- `DFS(G, w)`：对未访问邻接点继续递归。

### 3. 连通分量统计模板

若题目要求统计无向图的连通分量个数，标准写法如下：

```c
int CountComponent(ALGraph G) {
    int count = 0;
    for (int i = 0; i < G.vexnum; i++) {
        visited[i] = false;
    }
    for (int i = 0; i < G.vexnum; i++) {
        if (!visited[i]) {
            DFS(G, i);
            count++;
        }
    }
    return count;
}
```

关键理解：

- 每次从一个“尚未访问”的顶点启动 DFS，就能访问它所在的整个连通分量。
- 因此启动 DFS 的次数就是连通分量个数。
- `count++` 应放在一次 DFS 完成之后，而不是每访问一条边就加。

### 4. 参考填空思路

| 空号 | 参考答案 | 理由 |
| --- | --- | --- |
| (1) | `p->adjvex` | 取得边结点对应的邻接顶点 |
| (2) | `p = p->nextarc` | 沿邻接表访问下一条边 |
| (3) | `DFS(G, 0)` 或初始化 `count = 0` | 依代码上下文决定，通常是启动 DFS 或初始化计数 |
| (4) | `count++` | 发现一个新的连通分量后计数加一 |
| (5) | `return count` | 返回统计结果 |

应试提醒：第三空依赖完整代码上下文，必须看空位在函数开始处还是循环内部；如果在循环前，多半是初始化；如果在发现未访问顶点处，多半是 `DFS(G, i)`。

### 5. 本题通关模板

遇到图算法填空，先定位三件事：

1. 图的存储结构：邻接矩阵还是邻接表。
2. 遍历方式：DFS 递归、DFS 栈、BFS 队列。
3. 统计目标：顶点数、边数、连通分量、路径是否存在。

---

## 六、试题五：Java 策略模式代码补全解析

### 1. 考点定位

本题考查 **策略模式（Strategy Pattern）** 与 Java 基础语法。

业务变化点：同一个区间对象 `Intervals`，需要支持多种显示格式：

- `[lower,upper]`
- `[lower...upper]`
- `[lower-upper]`

策略模式的设计：

- 抽象策略：`PrintStrategy`
- 具体策略：`PrintIntervalsComma`、`PrintIntervalsDots`、`PrintIntervalsLine`
- 上下文：`Intervals`
- 工厂/选择逻辑：`getStrategy(TYPE type)` 根据枚举返回具体策略对象。

### 2. 代码结构理解

核心关系如下：

```text
Intervals.printInterval(PrintStrategy pr)
    ↓ 调用
pr.doPrint(this)
    ↓ 动态绑定到
PrintIntervalsComma / PrintIntervalsDots / PrintIntervalsLine
```

也就是说，区间对象不关心具体怎么打印，只把自己交给策略对象。

### 3. 参考填空

#### 空 (1)：接口方法声明

```java
void doPrint(Intervals val)
```

如果题目类名写作 `interval`，则应保持与题干一致：

```java
void doPrint(interval val)
```

解析：

- 接口中声明策略方法。
- 具体策略类都要实现这个方法。
- 参数是要打印的区间对象。

#### 空 (2)：上下文调用策略

```java
pr.doPrint(this)
```

解析：

- `this` 表示当前 `Intervals` 对象。
- 策略对象 `pr` 负责具体输出格式。

#### 空 (3)：逗号格式策略

```java
st = new PrintIntervalsComma()
```

#### 空 (4)：省略号格式策略

```java
st = new PrintIntervalsDots()
```

#### 空 (5)：短横线格式策略

```java
st = new PrintIntervalsLine()
```

完整关键片段可写为：

```java
interface PrintStrategy {
    void doPrint(Intervals val);
}

class Intervals {
    public void printInterval(PrintStrategy pr) {
        pr.doPrint(this);
    }
}

public static PrintStrategy getStrategy(TYPE type) {
    PrintStrategy st = null;
    switch (type) {
        case COMMA:
            st = new PrintIntervalsComma();
            break;
        case DOTS:
            st = new PrintIntervalsDots();
            break;
        case LINE:
            st = new PrintIntervalsLine();
            break;
    }
    return st;
}
```

### 4. 策略模式答题话术

如果问为什么用策略模式，可以这样答：

```text
区间显示格式存在多种可替换算法，且后续可能继续扩展。使用策略模式可将不同打印算法封装为独立策略类，Intervals 只依赖 PrintStrategy 接口，不依赖具体打印实现，从而降低耦合，便于扩展新的显示格式。
```

### 5. 本题易错点

- `PrintStrategy` 是接口，不应直接 `new PrintStrategy()`。
- `Intervals` 调用策略时传的是当前对象 `this`，不是 `lower` 和 `upper` 两个值。
- `switch` 每个分支都要创建对应的具体策略对象。
- 类名、方法名大小写要和题干一致，软考代码题通常按关键词给分。

---

## 七、如果选做 C++ 题，如何判断是否比 Java 题更适合你

因为下午最后一题通常是 Java 和 C++ 二选一，建议按以下标准选择：

| 情况 | 建议 |
| --- | --- |
| 熟悉 Java 接口、继承、多态、设计模式 | 优先 Java 题 |
| 熟悉 C++ 类、虚函数、指针、引用、STL | 可选 C++ 题 |
| 两门都一般 | 选题干更短、空更偏设计模式的一题 |
| Java 题出现策略/观察者/工厂等模式 | 通常比复杂指针题更容易拿分 |

下午题不是考你写完整工程，而是考你能否看懂上下文并补关键语句。

---

## 八、本套卷暴露出的高频考点清单

### 1. 必须会背且会用的图表题知识

- DFD：外部实体、加工、数据存储、数据流。
- 数据字典：`数据流 = 数据项 + 数据项 + ...`。
- ER 图：实体、属性、联系、联系属性、多重度。
- 关系模式：主键、外键、联合主键。
- UML 类图：泛化、关联、聚合、组合、依赖、实现。

### 2. 必须掌握的设计模式

| 模式 | 触发关键词 | 答题核心 |
| --- | --- | --- |
| 观察者模式 | 状态变化、通知、关注、订阅 | 一对多通知，降低主题和观察者耦合 |
| 策略模式 | 多种算法、多种格式、可替换 | 封装算法族，运行时选择策略 |
| 工厂方法 | 创建对象但不指定具体类 | 延迟对象创建到子类或工厂 |
| 抽象工厂 | 一族相关产品 | 创建产品族，保持产品一致性 |
| 单例模式 | 唯一实例 | 控制实例数量，全局访问点 |
| 适配器模式 | 接口不兼容 | 转换接口以复用已有类 |
| 装饰器模式 | 动态增加职责 | 比继承更灵活地扩展功能 |

### 3. 必须熟悉的数据结构代码

- 邻接表 DFS。
- 邻接矩阵 DFS/BFS。
- 连通分量统计。
- 栈、队列、链表的基本操作。
- 排序、查找、二叉树遍历。

---

## 九、考前 7 天复习计划

### Day 1：DFD 与数据字典

- 复习上下文图、0 层图平衡原则。
- 每天练 2 道 DFD 题。
- 总结常见外部实体和数据存储命名。

### Day 2：ER 图与关系模式

- 练习 `1:n`、`m:n`、三元联系转表。
- 强化主键、外键、联合主键写法。
- 重点检查联系属性归属。

### Day 3：UML 类图

- 复习泛化、实现、关联、聚合、组合。
- 做 2 道类名和属性识别题。
- 总结“包括、包含、拥有、使用”等关键词。

### Day 4：设计模式

- 背 8 个高频模式的意图和触发词。
- 每个模式准备一段 100 字以内答题话术。
- 重点掌握观察者、策略、工厂、适配器。

### Day 5：算法代码题

- 默写 DFS、BFS、连通分量模板。
- 练链表、栈、队列、二叉树遍历代码填空。
- 注意指针移动、递归出口、计数位置。

### Day 6：Java/C++ 选做题

- Java 方向：接口、抽象类、多态、策略模式、工厂模式。
- C++ 方向：类、虚函数、继承、引用、指针。
- 固定选择自己更稳的一门。

### Day 7：整套模拟

- 限时 150 分钟做一套下午题。
- 做完按“术语是否准确、主外键是否标注、模式解释是否结合题干”逐项检查。
- 把错题按题型归档，不要只记答案。

---

## 十、最后的应试提醒

1. 下午题一定要写得清楚，字迹不清可能直接丢分。
2. 不要空题。图表题即使不确定，也可以按题干关键词写出部分分。
3. 关系模式题中，主键和外键要主动标注，不要只写属性列表。
4. 设计模式题不要只写模式名，至少补一句“为什么适合本场景”。
5. 代码填空题填完后，按变量类型、方法名、返回值、循环推进四项检查。
6. 目标不是每题满分，而是稳定拿到 45 分以上；数据库、UML、设计模式、Java 题是提分重点。

---

## 参考资料

- 公开题目与参考答案来源之一：CSDN《2023年上半年软件设计师下午答案》。
- 公开题目文本来源之一：CSDN《2023年上半年 软件设计师答案解析》。
