# 2023 年下半年软件设计师下午试卷详细解析与通关笔记

> 适用目标：用“真题复盘 + 解题模板”的方式学习 2023 年下半年软件设计师下午案例题，重点训练 DFD、数据库设计、UML 状态图/类图、KMP 算法代码阅读与选做题应试能力。  
> 重要说明：本解析以公开题目/答案资料中的文字信息为基础进行学习化整理；涉及原试卷图形编号、箭头和多重度时，应以你手头原卷图片为最终校准。  
> 下午卷结构：前 4 题必做，每题 15 分；第 5、6 题二选一，通常分别对应 C++ 与 Java 或相近的面向对象程序设计题。

---

## 一、整套卷考点总览

| 题号 | 主题 | 核心考点 | 稳分关键 |
| --- | --- | --- | --- |
| 试题一 | 证券交易平台数据流图 | DFD 外部实体、数据存储、缺失数据流、系统边界调整 | 用题干原词命名；数据流起点/终点至少一端为加工 |
| 试题二 | 宾馆信息管理系统数据库设计 | ER 图、关系模式、主外键、范式问题 | 找实体主键；联系属性归位；识别传递依赖 |
| 试题三 | 自动售货机 UML 分析 | 用例规约、状态图、事件、类图 | 按业务流程排序状态；类名来自题干名词 |
| 试题四 | KMP 模式匹配算法 | next 数组、字符串匹配、C 代码填空、时间复杂度 | 理解 `i`、`j` 含义；记住失配时 `j = next[j]` |
| 试题五/六 | 面向对象程序设计 | C++/Java 代码阅读、接口/抽象类/多态、设计模式 | 先选熟悉语言；优先拿语法空和调用空 |

### 做题顺序建议

推荐顺序：

1. **试题二数据库设计**：主外键和范式题通常能拿稳定分。
2. **试题三 UML**：自动售货机业务直观，状态和类名容易从题干定位。
3. **试题一 DFD**：需要细看上下文图和 0 层图，注意平衡原则。
4. **试题五/六选做题**：选择自己更熟悉的语言。
5. **试题四 KMP**：若算法代码基础较弱，可最后做，先拿复杂度和返回值等确定分。

### 下午题通用检查清单

- DFD：外部实体是否在系统边界外？数据存储是否是系统内部长期保存的数据？
- ER：每个实体主键是否明确？`m:n` 或多元联系是否单独成表？
- UML：状态名是否来自题干？事件是否是“触发状态迁移”的动作/条件？
- 算法：循环条件是否保证不越界？返回值是 0 基还是 1 基？
- 代码：类名、方法名、变量名大小写是否与题干一致？

---

## 二、试题一：证券交易平台 DFD 解析

### 1. 题目业务梳理

证券交易平台的主要功能可以抽成 5 个加工：

1. **开户**：客户服务助理提交开户信息，系统建立客户记录和账户记录。
2. **存款**：客户向账户存款，系统根据存款金额修改账户余额。
3. **取款**：客户从账户取款，系统根据取款金额修改账户余额。
4. **证券交易**：客户可在线交易，经纪人可通过电话代办交易，系统保存交易信息。
5. **检查交易**：系统读取交易记录，将交易明细返回给客户。

DFD 题一定先拆出三类对象：

```text
外部实体：谁在系统外与平台交互？
数据存储：系统内部长期保存什么？
加工：平台内部做了哪些处理？
```

### 2. 问题 1：外部实体 E1-E3

参考答案：

| 编号 | 外部实体 | 定位依据 |
| --- | --- | --- |
| E1 | 客户服务助理 | 提交开户信息 |
| E2 | 客户 | 存款、取款、在线证券交易、查询交易明细 |
| E3 | 经纪人 | 通过电话方式进行证券交易 |

解析：

- “客户服务助理”只提交开户信息，不属于平台内部加工，所以是外部实体。
- “客户”既发起存款/取款/交易，也接收交易明细，是典型外部实体。
- “经纪人”通过电话参与交易，仍然是平台外部参与者。

应试提醒：不要把“证券交易平台”写成外部实体，它是系统本身；也不要把“账户记录”“交易记录”写成外部实体，它们是数据存储。

### 3. 问题 2：数据存储 D1-D3

参考答案：

| 编号 | 数据存储 | 说明 |
| --- | --- | --- |
| D1 | 客户记录 | 保存开户产生的客户信息 |
| D2 | 账户记录 | 保存账户余额等账户信息 |
| D3 | 交易记录 | 保存证券交易信息 |

解析：

- 题干直接给出“客户记录、账户记录、交易记录”三个存储名，应照抄题干原词。
- DFD 中数据存储一般是“记录、文件、表、库”等名词结构。
- “开户”不是数据存储，它是加工；“余额”只是账户记录中的数据项，不宜单独写成 D2。

### 4. 问题 3：补充缺失数据流

参考答案：

| 数据流名称 | 起点 | 终点 | 理由 |
| --- | --- | --- | --- |
| 修改账户余额 | 存款 | D2 账户记录 | 存款后要更新账户余额 |
| 修改账户余额 | 取款 | D2 账户记录 | 取款后也要更新账户余额 |
| 交易信息 | 证券交易 | D3 交易记录 | 交易完成后要保存交易信息 |

可接受写法：

- `存款后的账户余额`、`更新后的账户信息` 可以表达第一条。
- `取款后的账户余额`、`更新后的账户信息` 可以表达第二条。
- `交易信息存入交易记录` 可以表达第三条。

但最稳写法是：

```text
数据流名称：修改账户余额；起点：存款；终点：账户记录。
数据流名称：修改账户余额；起点：取款；终点：账户记录。
数据流名称：交易信息；起点：证券交易；终点：交易记录。
```

### 5. 问题 4：增加证券交易中心后的 DFD 修改

题干新增需求：实际证券交易通常在证券交易中心完成，平台需要把交易信息传递给证券交易中心。

参考答法：

```text
在上下文图和 0 层图中增加外部实体“证券交易中心”。证券交易加工产生的交易信息应发送给证券交易中心；检查交易时，若交易明细由证券交易中心确认或返回，则相应数据流应从证券交易中心流向检查交易加工，再由平台返回给客户。同时保持上下文图与 0 层图的数据流平衡。
```

解析：

- “证券交易中心”不是平台内部模块，而是外部机构，应建成外部实体。
- 修改 DFD 时不能只改 0 层图，上下文图也要同步增加与证券交易中心之间的数据流。
- 文字题要写出“新增外部实体”和“新增/调整数据流”两件事。

### 6. 本题通关模板

DFD 补图题可按以下四步做：

```text
第 1 步：圈出题干中的人、组织、其他系统 → 外部实体候选。
第 2 步：圈出“记录、表、文件、库” → 数据存储候选。
第 3 步：圈出“开户、存款、取款、交易、检查” → 加工候选。
第 4 步：用父图-子图平衡、输入输出守恒检查缺失数据流。
```

易错点：

- 数据流不能直接从外部实体流到数据存储，通常必须经过加工。
- 一个加工不能只有输入没有输出，也不能只有输出没有输入。
- 补数据流时尽量写“业务数据”，不要写“请求”“响应”这类过泛词，除非题干就是这样命名。

---

## 三、试题二：宾馆信息管理系统数据库设计解析

### 1. 题目实体与属性梳理

| 实体 | 主要属性 | 主键 |
| --- | --- | --- |
| 部门 | 部门号、部门名称、电话、经理 | 部门号 |
| 员工 | 员工号、姓名、岗位、电话、工资 | 员工号 |
| 客房 | 客房号、客房类型、收费标准、入住状态 | 客房号 |
| 客户 | 客户号、单位名称、联系人、联系电话、联系地址 | 客户号 |
| 预订申请 | 申请号、客户号、入住时间、入住天数、客房类型、客房数量 | 申请号 |
| 安排 | 客房号、姓名、性别、身份证号、入住时间、天数、电话、业务员 | 客房号 + 身份证号 + 入住时间 |

题干中的业务约束：

- 一个部门有多名员工，每名员工只属于一个部门。
- 每个部门只有一名经理负责管理。
- 一个客户可以有多个预订申请，一个预订申请只属于一个客户。
- 一名业务员可以安排多个预订申请，一个预订申请只由一名业务员安排，而且可安排多间同类型客房。

### 2. 问题 1：补充 ER 图联系

参考联系：

| 联系 | 参与实体 | 联系类型 | 说明 |
| --- | --- | --- | --- |
| 管理 | 经理/员工 与 部门 | 1:1 | 每个部门只有一名经理 |
| 隶属/属于 | 部门 与 员工 | 1:n | 一个部门多名员工，员工只属于一个部门 |
| 提交/拥有 | 客户 与 预订申请 | 1:n | 一个客户可有多个申请，申请只对应一个客户 |
| 安排 | 业务员、预订申请、客房 | 多元联系 | 业务员依据申请安排一间或多间客房 |

解析：

- “经理”在属性上是员工的一种岗位，但在 ER 图中可体现为员工参与“管理部门”的 1:1 联系。
- “部门-员工”是最典型的 `1:n`，通常在员工关系中加入 `部门号` 作外键。
- “客户-预订申请”也是 `1:n`，所以预订申请关系中要包含 `客户号`。
- “安排”涉及业务员、预订申请、客房，并带有住客身份信息、入住时间等明细，应作为独立关系。

### 3. 问题 2：补全关系模式空 (a)-(d)

题干给出的关系模式：

```text
部门（部门号，部门名称，经理，电话）
员工（员工号，（a），姓名，岗位，电话，工资）
客户（（b），联系人，联系电话，联系地址）
客房（客房号，客房类型，收费标准，入住状态）
预订申请（（c），入住时间，天数，客房类型，客房数量）
安排（申请号，客房号，姓名，性别，（d），天数，电话，业务员）
```

参考答案：

```text
(a) = 部门号
(b) = 客户号，单位名称
(c) = 申请号，客户号
(d) = 身份证号，入住时间
```

解析：

- 员工属于部门，所以员工表应加 `部门号` 作外键。
- 客户信息题干包含 `客户号、单位名称、联系人、联系电话、联系地址`，因此 (b) 缺少前两个属性。
- 预订申请由申请号唯一标识，并且对应一个客户，所以 (c) 是 `申请号、客户号`。
- 安排信息题干包含 `身份证号、入住时间`，并且这两个属性参与唯一标识，因此 (d) 是它们。

### 4. “预订申请”和“安排”的主键、外键

#### 预订申请

```text
预订申请（申请号，客户号，入住时间，天数，客房类型，客房数量）
主键：申请号
外键：客户号 → 客户(客户号)
```

说明：有些参考资料把“申请号”也写入外键列表，这通常是因为图中存在“安排”关系引用申请号；从关系模式本身看，预订申请表的外键应重点标 `客户号`。

#### 安排

```text
安排（申请号，客房号，姓名，性别，身份证号，入住时间，天数，电话，业务员）
主键：客房号 + 身份证号 + 入住时间
外键：申请号 → 预订申请(申请号)
外键：客房号 → 客房(客房号)
外键：业务员 → 员工(员工号)
```

解析：

- 题干明确“客房号、身份证号和入住时间唯一标识一次安排”，所以三者组成联合主键。
- `业务员` 应指向员工表中的员工号，而不是员工姓名。
- `申请号` 用于说明本次安排来自哪条预订申请。

### 5. 问题 3：客房关系模式是否存在规范性问题

结论：**存在规范性问题**。

原因：

```text
客房号 → 客房类型
客房类型 → 收费标准
因此：客房号 → 收费标准 是传递函数依赖
```

也就是说，`收费标准` 实际上由 `客房类型` 决定，而不是直接由某个客房号决定。把 `收费标准` 放在 `客房` 表中，会导致：

- 同一类型多间客房重复保存收费标准，产生数据冗余。
- 修改某类客房收费标准时要改多行，容易产生修改异常。
- 删除最后一间某类型客房时，可能丢失该类型的收费标准，产生删除异常。

推荐拆分：

```text
客房（客房号，客房类型，入住状态）
客房类型（客房类型，收费标准）
```

其中：

```text
客房.客房类型 → 客房类型.客房类型
```

### 6. 本题通关模板

数据库设计题可按这个顺序答：

```text
1. 先列实体和主键。
2. 再找 1:n 联系，把 1 端主键放到 n 端。
3. 遇到 m:n 或三元联系，单独建联系表。
4. 主键优先用题干中“唯一标识”的属性。
5. 范式题先写函数依赖，再指出部分依赖或传递依赖。
```

---

## 四、试题三：自动售货机 UML 状态图与类图解析

### 1. 业务流程重建

自动售货机购买饮料的流程：

1. 顾客选择饮料及数量。
2. 顾客投入硬币。
3. 硬币器收集硬币并计算金额。
4. 如果金额不足，提示继续投币或重新选择。
5. 如果金额足够，检查饮料储存数量。
6. 如果饮料不足，提示重新选择。
7. 如果饮料足够，推出饮料并计算找零。
8. 购买结束后清空硬币器，等待下一次交易。
9. 顾客也可按退币按钮退回已投入但尚未成功购买的硬币。

### 2. 问题 1：状态 S1-S4

参考答案：

| 编号 | 状态名 | 含义 |
| --- | --- | --- |
| S1 | 空闲 | 等待顾客开始下一次交易 |
| S2 | 准备服务 | 已选择/已投币，正在检查金额等条件 |
| S3 | 饮料出售 | 正在推出饮料或处理出货过程 |
| S4 | 可购买 | 金额足够，等待/执行库存检查后可出货 |

解析：

- 空闲状态通常是初始状态和一次交易完成后的回归状态。
- 投币后系统开始计算金额，应进入准备服务状态。
- 金额足够后才具备购买资格，因此进入可购买状态。
- 饮料数量足够后推出饮料，进入饮料出售状态。

### 3. 问题 2：事件 E1-E4

参考答案：

| 编号 | 事件名 | 触发含义 |
| --- | --- | --- |
| E1 | 饮料数量不足 | 库存不够，不能出货 |
| E2 | 硬币数量足够 | 投入金额达到购买要求 |
| E3 | 推出饮料 | 进入出货处理 |
| E4 | 返回找零 | 完成找零，交易结束 |

解析：

- 状态图中的事件是“导致状态迁移发生的触发条件或动作”。
- “硬币数量足够”触发从准备服务到可购买。
- “饮料数量不足”通常触发回到准备服务/重新选择。
- “返回找零”常作为交易完成后返回空闲状态的事件。

### 4. 问题 3：类图 C1-C5

参考答案：

| 编号 | 类名 | 说明 |
| --- | --- | --- |
| C1 | 自动售货机 | 系统核心对象，协调购买流程 |
| C2 | 硬币器 | 收集硬币、计算投入金额、退币/清空 |
| C3 | 饮料储存仓 | 管理饮料库存，判断数量是否足够 |
| C4 | 硬币 | 顾客投入的支付对象 |
| C5 | 饮料 | 被购买和推出的商品对象 |

解析：

- 题干明确出现了“自动售货机、硬币器、饮料储存仓、硬币、饮料”等业务名词，类图题优先照抄。
- 自动售货机一般组合/聚合硬币器和饮料储存仓。
- 硬币器处理硬币，饮料储存仓管理饮料，这种“名词 + 职责”对应很稳定。

### 5. 本题通关模板

状态图题：

```text
状态 = 系统在某一阶段的稳定情形，通常是名词/形容词短语。
事件 = 触发状态变化的动作或条件，通常是动词短语或判断结果。
```

类图题：

```text
类名优先来自题干名词：设备、部件、用户、商品、记录。
属性来自“信息包括……”。
方法来自“选择、投入、计算、检查、推出、退回”等动词。
```

---

## 五、试题四：KMP 模式匹配算法解析

### 1. KMP 的变量含义

题目给定：

- 主串：`t`，长度 `lt`。
- 子串/模式串：`s`，长度 `ls`。
- `next`：模式串的失配回退数组。
- `i`：主串当前比较下标。
- `j`：模式串当前比较下标。

KMP 的核心思想：

```text
匹配成功：i 和 j 都向后移动。
匹配失败：主串 i 不回退，只让模式串 j 回退到 next[j]。
```

### 2. 问题 1：代码填空

根据题干伪代码，`kmp` 主体应符合以下逻辑：

```c
int kmp(int *next, char *t, char *s, int lt, int ls) {
    int i = 0, j = 0;
    while (i < lt && j < ls) {
        if (j == -1 || t[i] == s[j]) {
            i++;
            j++;
        } else {
            j = next[j];
        }
    }
    if (j >= ls)
        return i + 1 - ls;
    else
        return -1;
}
```

参考填空：

| 空号 | 参考答案 | 解释 |
| --- | --- | --- |
| (1) | `j < ls` | 模式串还没有比较完时继续循环 |
| (2) | `t[i] == s[j]` | 当前主串字符与模式串字符相同 |
| (3) | `j = next[j]` | 失配时模式串回退，主串不回退 |
| (4) | `i + 1 - ls` | 返回从 1 开始计数的匹配起始位置 |

注意：有些排版资料会在 (3) 附近误混入 `get_next(next, s, ls)`，但 `get_next` 应在匹配前求好；在 `kmp` 的失配分支中，真正要填的是 `j = next[j]`。

### 3. 问题 2：时间复杂度

参考答案：

```text
O(lt + ls)
```

解析：

- 计算 `next` 数组需要扫描模式串，复杂度为 `O(ls)`。
- KMP 匹配过程中，主串下标 `i` 不回退，整体复杂度为 `O(lt)`。
- 合并后为 `O(lt + ls)`。

### 4. 问题 3：next 数组与返回值

模式串：

```text
BBABBCAC
```

按题目给出的改进 `next` 计算方式，结果为：

```text
-1, -1, 1, -1, -1, 2, 0, 0
```

主串：

```text
AABBCBBABBCACCD
```

子串：

```text
BBABBCAC
```

匹配位置分析：

```text
主串下标（0 基）：5
主串位置（1 基）：6
```

因此 `kmp` 返回值是：

```text
6
```

### 5. 手工算 next 的方法

对于改进 next 数组，不要死背，按下面规则：

```text
next[0] = -1
若 j == -1 或 s[i] == s[j]：i++，j++
    若 s[i] == s[j]，则 next[i] = next[j]
    否则 next[i] = j
否则：j = next[j]
```

手工考试时，建议在草稿纸画三列：

```text
i | j | next[i]
```

每次只记录 `i` 增加后的新 `next[i]`，这样不容易乱。

### 6. KMP 易错点

- `j == -1` 时必须让 `i++`、`j++`，否则会死循环。
- 失配时是 `j = next[j]`，不是 `i = next[i]`。
- 返回值要看题目要求：本题要求“从 1 开始”，所以返回 `i + 1 - ls`。
- 若是 C 语言真实代码，`get_next` 的循环要避免访问 `s[ls]` 越界；考试主要考伪代码逻辑，不深究编译运行。

---

## 六、试题五/六：选做程序设计题备考解析

> 说明：公开预览资料中，2023 年下半年下午卷的前四题信息较完整；第 5、6 题不同资料常以下载附件、视频或图片形式呈现。这里按软件设计师下午卷选做题的固定考法，给出可直接套用的读题和补代码方法。你若把原题图片发给我，我可以再按原代码逐空精确补充。

### 1. 选做题选择策略

| 你的基础 | 建议 |
| --- | --- |
| Java 更熟 | 选 Java 题，优先利用接口、抽象类、多态、设计模式关键词 |
| C++ 更熟 | 选 C++ 题，注意虚函数、继承、指针/引用、构造函数 |
| 两门都一般 | 先看哪题空更偏“类名/方法名/调用语句”，少选复杂指针题 |
| 设计模式题明显 | 优先选设计模式题，拿分通常比纯算法代码更稳 |

### 2. Java 代码填空通用模板

常见空位：

```java
interface Strategy {
    void method(Context ctx);
}

class Context {
    private Strategy strategy;

    public void setStrategy(Strategy strategy) {
        this.strategy = strategy;
    }

    public void execute() {
        strategy.method(this);
    }
}
```

如果题目出现：

- 多种算法/多种显示方式/多种计费方式 → 策略模式。
- 状态变化通知多个对象 → 观察者模式。
- 不直接 new 具体类，而通过方法创建 → 工厂方法或简单工厂。
- 一系列相关产品对象 → 抽象工厂。

### 3. C++ 代码填空通用模板

常见考点：

```cpp
class Base {
public:
    virtual void operation() = 0;
    virtual ~Base() {}
};

class Concrete : public Base {
public:
    void operation() override {
        // 具体实现
    }
};
```

答题时重点检查：

- 基类析构函数是否为 `virtual`。
- 纯虚函数是否写成 `= 0`。
- 子类继承是否使用 `public`。
- 通过基类指针/引用调用子类方法是否体现多态。
- 如果有容器遍历，迭代器类型与对象类型是否一致。

### 4. 程序设计题拿分顺序

1. 先补类名、接口名、方法名这些“题干原词空”。
2. 再补对象创建：`new ConcreteClass()`。
3. 再补多态调用：`obj.method(...)`。
4. 最后处理复杂条件、循环、指针等高风险空。

### 5. 设计模式答题话术

#### 策略模式

```text
该场景中存在多种可替换算法/处理方式。将每种算法封装为独立策略类，使上下文类只依赖抽象策略接口，可在运行时切换具体策略，降低耦合并便于扩展。
```

#### 观察者模式

```text
当一个对象状态变化时，需要自动通知多个依赖对象。将被观察对象与观察者解耦，支持观察者动态注册、注销，并在状态变化时统一通知。
```

#### 工厂方法/简单工厂

```text
对象创建逻辑较复杂或具体类型可能变化。通过工厂封装创建过程，使客户端不直接依赖具体类，提高扩展性和可维护性。
```

---

## 七、本套卷高频知识点归纳

### 1. DFD 高频规则

- 外部实体：系统边界外的人、组织、其他系统。
- 加工：动词 + 名词，如开户、存款、取款、证券交易、检查交易。
- 数据存储：系统内部保存的数据，如客户记录、账户记录、交易记录。
- 数据流：必须至少一端连接加工，不能外部实体直接连数据存储。
- 父图和子图必须保持输入输出平衡。

### 2. 数据库设计 高频规则

- “唯一标识”通常就是主键提示。
- `1:n`：在 n 端加入 1 端主键作外键。
- `m:n`：单独建联系表。
- 三元/多元联系：单独建联系表，包含各实体主键和联系属性。
- 范式题：先写函数依赖，再说明部分依赖或传递依赖。

### 3. UML 高频规则

- 用例图：参与者是系统外部角色。
- 状态图：状态是稳定阶段，事件是触发迁移的动作/条件。
- 类图：类名来自题干名词，方法来自题干动词。
- 组合/聚合：整体与部分，如自动售货机包含硬币器、饮料储存仓。

### 4. 算法题 高频规则

- 先写变量含义，不急着填空。
- 循环条件通常与数组长度相关。
- 递归/循环中必须有推进语句。
- 时间复杂度要把预处理和主过程一起算。
- 字符串匹配题一定区分 0 基下标与 1 基位置。

---

## 八、考前复习建议

### Day 1：DFD

- 练 2 道上下文图 + 0 层图补图题。
- 每题都写“外部实体、数据存储、缺失数据流”三张表。

### Day 2：数据库设计

- 重点练 ER 图转关系模式。
- 每个关系模式都主动标主键、外键。
- 专门练“传递依赖导致不满足 3NF”的题。

### Day 3：UML

- 复习用例图、状态图、类图。
- 背状态图关键词：空闲、准备、处理中、完成、异常。

### Day 4：算法

- 默写 KMP 主过程。
- 手算 3 个模式串的 next 数组。
- 复习 DFS、BFS、连通分量，避免只会 KMP。

### Day 5：选做语言

- Java：接口、抽象类、继承、多态、常见设计模式。
- C++：继承、虚函数、指针、引用、析构函数、STL 基础。

### Day 6：整套限时

- 150 分钟完成一套下午题。
- 做完按“术语准确、主外键完整、代码边界正确”复盘。

### Day 7：错题回炉

- 把所有错题归类为：读题遗漏、图关系错误、主外键错误、代码语法错误、算法理解错误。
- 每类整理 1 条下次避免策略。

---

## 九、最后冲刺提醒

1. 下午卷不要追求每题满分，目标是稳定过 45 分。
2. 数据库题、UML 题通常是提分核心，尽量拿到 10 分以上。
3. DFD 题缺失数据流必须写清“名称、起点、终点”。
4. 范式题必须出现“函数依赖、传递依赖、冗余、异常、拆表”等关键词。
5. 算法题不会全填也不要空着，循环条件、复杂度、返回值常常能拿基础分。
6. 选做题先看题号，别涂错；先选语言，再选空，不要两题都写导致答题卡混乱。

---

## 参考资料

- 人人文库：《2023年下半年软件设计师真题 答案解析(上午选择 下午案例)全国计算机软考》：https://www.renrendoc.com/paper/300473795.html
- CSDN：《「软件设计师」2023年下半年下午真题解析》：https://blog.csdn.net/m0_46322965/article/details/148051657
