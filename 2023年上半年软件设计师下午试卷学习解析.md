# 2023 年上半年软件设计师下午试卷学习解析

> 适用对象：正在备考软考中级“软件设计师”的考生。  
> 使用方式：先独立做题，再对照本解析检查“为什么这样答”，最后按文末清单复盘。  
> 说明：本文件按网络公开回忆版题干整理，原卷中的部分图形未随仓库提供；涉及图中编号的答案请以你手上的原卷图为准，本解析重点放在**解题套路、得分点和复习迁移**。

## 一、下午卷总体策略

软件设计师下午卷满分 75 分，通常为 4 道必答题 + 1 道选答题。下午卷不是单纯背诵题，而是考“把基础知识落到建模、设计、代码填空”的能力。

### 1. 建议答题顺序

1. **先扫全卷 3 分钟**：标出你最熟的题型，如数据库、UML、Java/C++ 代码题。
2. **先拿确定分**：数据库设计、设计模式代码填空通常可通过套路拿分。
3. **数据流图题不要死磕图**：从题干名词和动词出发，实体、数据存储、加工、数据流基本都能定位。
4. **代码题按“接口—实现—调用”顺序补空**：不要被大小写或题干 OCR 错误干扰，抓住设计模式结构。

### 2. 下午题通用得分口诀

| 题型 | 核心口诀 | 常见失分点 |
| --- | --- | --- |
| 数据流图 | 外部实体是系统外的人/系统；数据存储是系统内长期保存的信息；数据流用题干名词短语 | 把“处理动作”写成数据存储；漏写起点/终点 |
| 数据库 E-R | 实体看名词，联系看动词；多对多联系要转关系表；联系属性放到联系关系中 | 主键不完整；外键不标；三元联系拆错 |
| UML/面向对象 | 先找继承层次，再找组合/聚合，再找设计模式 | 类名和属性混写；模式只写名称不解释原因 |
| 程序设计/设计模式 | 接口定义行为；上下文持有或接收策略；工厂/选择器返回具体策略对象 | 方法签名不一致；忘记 `new`；调用对象写错 |

---

## 二、试题一：数据流图与结构化分析——农事管理服务平台

### 1. 考点定位

本题考查结构化分析中的：

- 上下文数据流图：识别系统边界外的外部实体；
- 0 层数据流图：识别主要加工、数据存储和数据流；
- 数据流组成：将一个复合数据流拆成若干数据项。

题干业务是“农事管理服务平台”，核心功能包括人员管理、基地管理、种植管理、投入品管理和信息服务。

### 2. 问题 1：外部实体 E1—E4

根据题干角色，系统外部交互者主要有：平台管理员、租户、农户、第三方软件。

参考填法如下：

| 编号 | 参考答案 | 判断依据 |
| --- | --- | --- |
| E1 | 平台管理员 | 负责管理租户，是系统外部管理角色 |
| E2 | 农户 | 接收农事提醒、记录农事活动、填写投入品消耗 |
| E3 | 租户 | 管理农户、地块、作物和农事过程 |
| E4 | 第三方软件 | 通过 APP/API 接口访问系统并发起请求 |

> 学习要点：外部实体一定在系统边界之外，通常是“人、组织、外部系统”。不要把“农事过程”“地块”这类被系统管理的数据当成外部实体。

### 3. 问题 2：数据存储 D1—D4

数据存储一般对应题干中需要长期保存、反复读写的信息。可按功能模块归纳：

| 编号 | 参考答案 | 可接受写法 | 来源功能 |
| --- | --- | --- | --- |
| D1 | 人员信息 | 人员表、用户信息表、租户/农户信息 | 人员管理 |
| D2 | 基地及地块信息 | 基地地块表、地块信息、基地信息 | 基地管理 |
| D3 | 农事过程信息 | 农事过程表、作物农事过程、农事计划 | 种植管理 |
| D4 | 农事活动记录 | 农事活动表、农事实施记录、投入品消耗记录 | 农户实施与记录 |

> 学习要点：数据存储命名尽量用“名词 + 信息/表/记录”。如果题目要求“使用说明中的词语”，就尽量照抄题干中的业务词。

### 4. 问题 3：补充缺失数据流

由于原始 0 层图未在仓库中提供，本题无法逐一核对图上缺失箭头编号。考试时可按下面流程补全：

1. **先找加工的输入输出是否平衡**：上下文图中进入系统的数据流，0 层图中必须能找到对应入口；从系统出去的数据流也要能对应。
2. **按功能定位数据流**：
   - 人员管理：租户信息、农户信息、地块分配信息；
   - 基地管理：基地描述信息、地块绘制信息；
   - 种植管理：作物信息、农事过程、农事活动计划、农事提醒、逾期告警；
   - 投入品管理：投入品信息、投入品实际消耗；
   - 信息服务：农事信息请求、查询条件、农事响应信息。
3. **起点/终点必须写清楚**：例如“租户 → 种植管理：农事过程设定信息”“农户 → 种植管理：农事活动记录”。

常见可补的数据流包括：

| 数据流 | 起点 | 终点 | 理由 |
| --- | --- | --- | --- |
| 农户信息/地块分配信息 | 租户 | 人员管理 | 租户管理农户并分配地块 |
| 基地及地块描述信息 | 租户 | 基地管理 | 租户填写基地名称、地域并绘制地块 |
| 农事过程/农事活动计划 | 租户 | 种植管理 | 租户设定作物及农事过程 |
| 农事活动记录/投入品消耗 | 农户 | 种植管理或投入品管理 | 农户实施农事活动并记录实际消耗 |
| 农事提醒 | 种植管理 | 农户 | 系统按计划提醒农户 |
| 逾期告警 | 种植管理 | 租户 | 活动逾期未实施时告警 |
| 农事信息请求 | 用户/第三方软件 | 信息服务 | 查询、对比、统计农事信息 |
| 农事响应信息 | 信息服务 | 用户/第三方软件 | 返回查询结果或特定格式信息 |

### 5. 问题 4：“农事信息请求”的组成

题干明确描述了信息服务：用户或第三方软件可提供账号、密码、查询条件发起请求；无查询条件时默认返回账号下所有信息，多查询条件时返回满足全部条件的信息。

因此可写：

```text
农事信息请求 = 账号 + 密码 + 查询条件
```

如果按系统内用户查询，则也可写：

```text
农事信息请求 = 用户标识 + 查询条件
```

更完整的学习型答案：

```text
农事信息请求 = 请求方身份信息（账号、密码或用户标识） + 查询条件集合
```

### 6. 本题复盘

- 做数据流图题时，先划出题干中的**角色名词**、**业务数据名词**、**处理动词**。
- 外部实体来自角色名词；数据存储来自业务数据名词；加工来自处理动词。
- 起点/终点不要写反：谁提交信息，谁就是起点；谁接收结果，谁就是终点。

---

## 三、试题二：数据库设计——汽车零件采购系统

### 1. 考点定位

本题考查：

- E-R 图补充；
- 关系模式转换；
- 主键、外键、联系属性；
- 新需求下的模型扩展。

题干中的实体有：供应商、零件、车型。采购是三者之间的联系，并带有采购数量、采购日期等属性。

### 2. 问题 1：补充 E-R 图

#### 实体与属性

| 实体 | 属性 | 主标识建议 |
| --- | --- | --- |
| 供应商 | 名称、地址、电话 | 名称 |
| 零件 | 编码、名称、价格 | 编码 |
| 车型 | 编号、名称、规格 | 编号 |

#### 联系

采购关系连接三个实体：

```text
车型 —— 采购 —— 零件
           |
        供应商
```

联系属性：

```text
采购数量、采购日期
```

联系类型理解：

- 一个车型可采购多种零件；
- 一个零件可被多个车型采用；
- 一个供应商可供应多种零件；
- 某车型的某零件可从多家供应商采购。

所以本题的采购本质上是**供应商、零件、车型之间的多对多对多三元联系**。

> 易错点：不要把采购简单画成“供应商—零件”的二元联系，因为题干强调“某个车型的某种零件可以从多家供应商采购”，车型也参与决定采购事实。

### 3. 问题 2：补充关系模式

题干给出：

```text
采购(车型编号，供应商名称，(a)，(b)，采购日期)
```

根据采购关系属性，缺失项应为：

```text
(a) 零件编码
(b) 采购数量
```

推荐关系模式：

```text
供应商(名称, 地址, 电话)
  主键：名称

零件(编码, 名称, 价格)
  主键：编码

车型(编号, 名称, 规格)
  主键：编号

采购(车型编号, 供应商名称, 零件编码, 采购数量, 采购日期)
  主键：车型编号 + 供应商名称 + 零件编码 + 采购日期
  外键：车型编号 → 车型(编号)
  外键：供应商名称 → 供应商(名称)
  外键：零件编码 → 零件(编码)
```

> 关于主键：如果题目默认“同一车型、同一供应商、同一零件只记录一条采购信息”，则主键也可写为 `车型编号 + 供应商名称 + 零件编码`。但从真实业务和“采购日期”属性看，同一组合可能多次采购，因此加入采购日期更稳妥。

### 4. 问题 3：新增门店销售需求

新增需求：记录车型在全国门店的销售情况，门店信息包括门店编号、地址、电话，销售包括销售数量和销售日期。

#### E-R 图新增内容

新增实体：

```text
门店(门店编号, 地址, 电话)
```

新增联系：

```text
门店 —— 销售 —— 车型
```

联系属性：

```text
销售数量、销售日期
```

联系类型：

- 一个门店可销售多种车型；
- 一个车型可在多个门店销售；
- 因此门店与车型之间是多对多联系。

#### 新增关系模式

```text
门店(门店编号, 地址, 电话)
  主键：门店编号

销售(门店编号, 车型编号, 销售数量, 销售日期)
  主键：门店编号 + 车型编号 + 销售日期
  外键：门店编号 → 门店(门店编号)
  外键：车型编号 → 车型(编号)
```

> 易错点：销售数量、销售日期是“销售联系”的属性，不是门店实体或车型实体的固有属性。

### 5. 本题复盘

- 多对多联系转换成关系表时，关系表的主键通常由参与实体的主键组合而成。
- 三元联系不要随意拆成三个二元联系，否则会丢失业务语义。
- 带时间的业务记录，如果允许重复发生，时间常常也要参与主键，或另设流水号。

---

## 四、试题三：面向对象分析与设计——数字图书馆系统

### 1. 考点定位

本题考查：

- 从需求描述识别类；
- 继承关系建模；
- 类的关键属性；
- 设计模式选择与解释。

题干领域包括：镜像数据库、学术资源、会议论文、期刊论文、学位论文、会议集、期刊期号、用户、学生、教师、在职人员，以及按题名/作者/来源检索。

### 2. 类识别思路

#### 资源类层次

```text
Resource（学术资源）
├── ConferencePaper（会议论文）
├── JournalArticle（期刊论文）
└── Thesis（学位论文）
```

#### 用户类层次

```text
User（用户）
├── Student（学生）
├── Teacher（教师）
└── Staff（其他在职人员）
```

#### 容器/来源类

```text
MirrorDB / MinorDB（镜像数据库）
Proceeding（会议集）
Edition（期刊某一期）
```

### 3. 问题 1：C1—C8 类名参考

由于原始类图编号未随仓库提供，下面给出按常见类图布局的参考映射。你应对照手头原卷图中的继承箭头和关联关系调整编号。

| 类名 | 需求依据 | 典型关系 |
| --- | --- | --- |
| Resource | 每个学术资源有题名、作者、发表时间等公共信息 | 会议论文、期刊论文、学位论文的父类 |
| ConferencePaper | 会议论文需记录会议名称、召开时间、地点 | Resource 子类，属于 Proceeding |
| JournalArticle | 期刊论文需记录期刊名称、出版月份、期号、主办单位 | Resource 子类，属于 Edition |
| Thesis | 学位论文需记录学位类别、学校、专业、导师 | Resource 子类 |
| Proceeding | 同一次会议论文被收录在会议集中 | 包含多篇 ConferencePaper |
| Edition | 同一期号论文被收录在一本期刊中 | 包含多篇 JournalArticle |
| User | 用户统一身份认证登录系统 | Student、Teacher、Staff 的父类 |
| MirrorDB / MinorDB | 镜像数据库保存资源来源信息 | 包含或关联多个 Resource |

若类图中 C1—C8 只标注了资源与用户相关类，则常见答案可能是：

```text
C1 Resource
C2 ConferencePaper
C3 JournalArticle
C4 Thesis
C5 User
C6 Student
C7 Teacher
C8 Staff
```

若类图中包含资源容器类，则常见答案可能是：

```text
C1 MirrorDB
C2 Resource
C3 ConferencePaper
C4 JournalArticle
C5 Thesis
C6 Proceeding
C7 Edition
C8 User
```

考试时判断方法：

- 有 3 个子类继承自同一个父类，且子类属性分别对应会议、期刊、学位，则父类是 Resource。
- 有 3 个子类继承自同一个父类，且子类是学生、教师、职工，则父类是 User。
- 与会议论文“一对多/包含”关联的是 Proceeding。
- 与期刊论文“一对多/包含”关联的是 Edition。

### 4. 问题 2：关键属性

按需求提取属性，公共属性放父类，特有属性放子类。

#### Resource

```text
题名、作者、发表时间、来源、被引次数、下载次数
```

#### ConferencePaper

```text
会议名称、召开时间、召开地点
```

#### JournalArticle

```text
期刊名称、出版月份、期号、主办单位
```

#### Thesis

```text
学位类别、毕业学校、专业、指导教师
```

#### MirrorDB

```text
数据库名称、访问地址、数据库属性、数据库简介
```

#### User

```text
用户标识、姓名、统一身份认证账号
```

> 得分技巧：如果问“关键属性”，不要把方法写进去；如果问“操作/服务”，才写 browse、search、download 等。

### 5. 问题 3：新增“他引通知”需求的设计模式

参考答案：**观察者模式（Observer Pattern）**。

#### 为什么是观察者模式？

新增需求是：用户可以对关注的数字资源注册他引通知；当资源被引次数发生变化时，系统及时通知用户。

这正好符合观察者模式：

- 被观察者 Subject：数字资源 Resource；
- 观察者 Observer：注册通知的用户 User；
- 注册：用户订阅某个资源；
- 状态变化：Resource 的被引次数变化；
- 通知：Resource 通知所有已注册用户。

#### 150 字以内答题模板

```text
可采用观察者模式。数字资源作为被观察目标，用户作为观察者。用户对关注的资源注册通知后，当资源的被引次数发生变化，资源对象自动通知所有已注册用户。该模式能降低资源与用户之间的耦合，支持多个用户订阅同一资源，也便于后续扩展通知方式。
```

### 6. 本题复盘

- 看到“状态变化后通知多个对象”，优先想到观察者模式。
- 看到“多种算法可替换”，优先想到策略模式。
- 看到“复制已有对象创建新对象”，优先想到原型模式。
- 看到“只允许一个实例”，优先想到单例模式。

---

## 五、试题四：Java 代码填空——策略模式打印区间

### 1. 考点定位

本题考查策略模式。题干要求同一个区间对象支持多种打印格式：

```text
[lower, upper]
[lower...upper]
[lower-upper]
```

策略模式结构：

```text
Context：Intervals
Strategy：PrintStrategy
ConcreteStrategy：PrintIntervalsComma / PrintIntervalsDots / PrintIntervalsLine
```

### 2. 标准代码骨架

为了理解填空，可整理成如下更规范的 Java 代码：

```java
enum TYPE { COMMA, DOTS, LINE }

interface PrintStrategy {
    void doPrint(Intervals val);
}

class Intervals {
    private double lower;
    private double upper;

    public Intervals(double lower, double upper) {
        this.lower = lower;
        this.upper = upper;
    }

    public double getLower() {
        return lower;
    }

    public double getUpper() {
        return upper;
    }

    public void printInterval(PrintStrategy pr) {
        pr.doPrint(this);
    }
}

class PrintIntervalsComma implements PrintStrategy {
    public void doPrint(Intervals val) {
        System.out.println("[" + val.getLower() + "," + val.getUpper() + "]");
    }
}

class PrintIntervalsDots implements PrintStrategy {
    public void doPrint(Intervals val) {
        System.out.println("[" + val.getLower() + "..." + val.getUpper() + "]");
    }
}

class PrintIntervalsLine implements PrintStrategy {
    public void doPrint(Intervals val) {
        System.out.println("[" + val.getLower() + "-" + val.getUpper() + "]");
    }
}

public static PrintStrategy getStrategy(TYPE type) {
    PrintStrategy st = null;
    switch (type) {
        case COMMA:
            st = new PrintIntervalsComma();
            break;
        case DOTS:
            st = new PrintIntervalsDots();
            break;
        case LINE:
            st = new PrintIntervalsLine();
            break;
    }
    return st;
}
```

### 3. 填空答案

| 空号 | 参考答案 | 解析 |
| --- | --- | --- |
| (1) | `void doPrint(Intervals val)` | 策略接口声明统一打印方法 |
| (2) | `pr.doPrint(this)` | 上下文对象调用传入策略，把自己作为参数传给策略 |
| (3) | `st = new PrintIntervalsComma()` | 逗号格式策略对象 |
| (4) | `st = new PrintIntervalsDots()` | 省略号格式策略对象 |
| (5) | `st = new PrintIntervalsLine()` | 短横线格式策略对象 |

> 如果原题类名写作 `interval` 或 `Intervals`，答题时应与题干保持一致。软考代码填空一般看结构和语义，不建议自行改类名。

### 4. 本题复盘

策略模式题的固定套路：

1. 接口中定义算法方法；
2. 每个具体策略类实现该方法；
3. 上下文类不自己判断格式，而是调用策略对象；
4. 客户端或工厂方法根据参数选择具体策略。

### 5. 可迁移考点

| 需求关键词 | 对应设计模式 |
| --- | --- |
| 多种格式、多种算法、运行时切换 | 策略模式 |
| 状态变化通知订阅者 | 观察者模式 |
| 克隆已有对象 | 原型模式 |
| 构造复杂对象且分步骤 | 建造者模式 |
| 统一创建对象，隐藏具体类 | 工厂方法/抽象工厂 |

---

## 六、选答题备考建议

2023 年上半年下午卷选答题通常在 C/C++ 与 Java 方向中选择。若你基础一般，建议优先选择自己最熟的语言；若两门都一般，建议选择 Java 设计模式题，因为它常见套路更固定。

### 1. Java 题准备清单

必须会写或看懂：

- `interface`、`implements`；
- 抽象类和继承；
- 构造方法；
- 简单工厂/策略/观察者/单例/原型；
- `switch` 返回不同对象；
- `this` 的含义。

### 2. C/C++ 题准备清单

必须掌握：

- 数组、字符串、指针；
- 递归；
- 栈、队列、链表、二叉树；
- 排序和查找；
- 时间复杂度；
- 边界条件。

### 3. 选题原则

| 情况 | 建议 |
| --- | --- |
| 能看懂 Java 类图和设计模式 | 优先 Java |
| 算法基础较好、C 语法熟 | 可选 C/C++ |
| 两道都能做一半 | 先做空更少、模板更明显的 |
| 代码题看不懂 | 先找方法名、变量名、返回值，不要逐字符纠错 |

---

## 七、整卷知识点地图

| 试题 | 主题 | 必背知识点 | 练习方法 |
| --- | --- | --- | --- |
| 试题一 | 数据流图 | 外部实体、加工、数据存储、数据流平衡 | 每天画 1 个 0 层 DFD |
| 试题二 | 数据库设计 | E-R 图、三元联系、主外键、关系模式 | 把需求名词转实体，动词转联系 |
| 试题三 | UML/OOD | 类识别、继承、关键属性、观察者模式 | 对业务描述画类图 |
| 试题四 | Java/设计模式 | 策略模式、接口、多态、工厂选择 | 默写策略模式模板 |
| 选答题 | 程序设计 | C/C++ 或 Java 语法与算法 | 只选一门主攻 |

---

## 八、考前 7 天复习计划

### 第 1 天：数据流图

- 背 DFD 四要素；
- 做 2 道历年试题一；
- 重点练“数据流起点/终点”。

### 第 2 天：数据库设计

- 复习 1:1、1:n、m:n 联系转换规则；
- 练三元联系；
- 每题都写主键和外键。

### 第 3 天：UML 类图

- 复习继承、实现、关联、聚合、组合、依赖；
- 根据需求抽类、抽属性；
- 整理常考设计模式。

### 第 4 天：设计模式

重点背 5 个模式：

1. 策略模式；
2. 观察者模式；
3. 工厂方法；
4. 原型模式；
5. 单例模式。

### 第 5 天：程序题

- 选择 Java 或 C/C++ 一个方向主攻；
- Java 方向默写策略模式、观察者模式；
- C/C++ 方向练字符串、数组、递归、树。

### 第 6 天：完整模拟

- 严格 150 分钟做一套下午卷；
- 不会的题也要写关键词；
- 对答案后整理“失分原因”。

### 第 7 天：错题回炉

- 只看错题和模板；
- 不再扩展新知识；
- 背常见答题句式。

---

## 九、可直接背的答题模板

### 1. 数据流图实体判断模板

```text
外部实体是位于系统边界之外、与系统交换数据的人、组织或外部系统。
```

### 2. 数据库联系转换模板

```text
多对多联系转换为独立关系模式，其属性包括参与实体的主键以及联系自身属性，主键通常由参与实体主键组合而成，参与实体主键同时作为外键。
```

### 3. 观察者模式模板

```text
当一个对象状态改变需要自动通知多个依赖对象时，可采用观察者模式。被观察对象维护观察者列表，状态变化时通知所有观察者，从而降低主题与观察者之间的耦合。
```

### 4. 策略模式模板

```text
当系统需要在运行时选择多种算法或行为之一时，可采用策略模式。将算法封装为独立策略类，通过统一策略接口调用，使算法变化独立于使用算法的客户类。
```

---

## 十、资料来源与核对建议

本解析结合公开回忆版题干和软考常见评分思路整理，建议你最终以自己手中的原始试卷图和官方/权威解析为准。复习时不要只背答案，应重点掌握“题干如何映射到模型”。

参考核对来源：

- CSDN 公开回忆版：`2023年上半年 软件设计师答案解析`
- 希赛网公开回忆版：`2023上半年软件设计师下午真题答案（四）`

